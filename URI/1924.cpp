#include <bits/stdc++.h> 

using namespace std;

int main() {
    
    ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int n, i;
    string word;
    cin >> n;
    getline(cin,word);
    for( i=0; i<n; i++ ) {
        getline(cin,word);
    }
    cout << "Ciencia da Computacao\n";
    return 0;
}
